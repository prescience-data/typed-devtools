export interface SchemasTemplate {
  requests: string[]
  events: string[]
}

export interface ConstantsTemplate {
  domains: string[]
}

export interface TypesTemplate {
  defs: string[]
  resultLookup: string[]
  paramsLookup: string[]
}
